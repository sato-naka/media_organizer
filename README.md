# たくさん画像移動整理君
## 概要
特定のフォルダから特定のフォルダへ日付ごとにフォルダ分けして画像や動画を振り分けるためのアプリケーションです

## セットアップ
- exiftool をダウンロードして exe ファイルと `exiftool_files` フォルダを同じフォルダに入れる
- その時 exe ファイルは `(-k)` の名前を外す

## 使い方
- `poetry run python app.py` で実行
- アプリケーションが立ち上がるので入力フォルダと出力フォルダなどを指定して `実行`